[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "discharge-rag"
version = "0.1.0"
description = "DischargeRAG full proposal implementation"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
